#implement in tcl, perl, lisp, c++, ruby, python, javascript
global quit #list to hold process that quit, every funcitn checks this before
global ifr_ip_
global ifr_port



int handler #exit gracefully

executing

establish connection ifr
	spawn telnet adddress

ifr init
	do_nothing manually configure 
estabolish connecton ardhino
arduino init

while (no fail and no error and no quit)

{
	txtest
	rxtest
	idle test
}

clean up



txtest
{
	key radio
	start_timer
	check freq error
	check tx power
	check arduino voltage
	if (endtimer)
		unkey radio
}

rxtest
{
	enable rfgen
	start_time
	check sinad
	if (endtimer)
		unkey radio
}


idle test
{	start_timer
	log shit
	info status
	if (endtimer)
	 return
}



checkerror
	{ if error
		exit _ gracefully
	}
